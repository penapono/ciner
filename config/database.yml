default: &default
  adapter: mysql2
  encoding: utf8
  host: localhost
  username: root
  password:
  timeout: 5000

test:
  <<: *default
  database: ciner_test

development:
  <<: *default
  database: ciner_development

production:
  <<: *default
  database: ciner_production
