.container.content
  .row
    .col-xs-12
      .title-separator
        .media
          .media-left.media-middle
            %svg.icon-title
              %use{"xlink:href" => "#critica"}
          .media-body.media-bottom
            .media-heading.title-section
              = Critic.model_name.human(count: 2)

  = render 'filters'

  = render 'highlight_critic', critic: critics.first

  = render 'critics', critics: critics
