- role = current_role(current_user, request.url)

= render 'shared/breadcrumbs'

.container
  .row
    .col-xs-9
      = render 'shared/series/toolbar'
      = render 'shared/series/index/playing', role: role
      = render 'shared/series/index/featured', role: role
      = render 'shared/series/index/playing_soon', role: role
      = render 'shared/series/index/available_netflix', role: role
      = render 'shared/series/index/available_amazon', role: role
    .col-xs-3
      = render 'shared/series/index/birthdays', role: role
