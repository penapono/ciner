- role = role
- showcase_class ||= 'showcase-poster'

/ MOVIES THUMB
.hidden-xs.visible-lg.visible-md{ class: showcase_class }
  - playing_movies.each do |pm|
    = render 'shared/home/showcase/item', role: role, pm: pm

/ MOVIES THUMB
