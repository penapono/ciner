.row.actions
  - if f.object.persisted?
    .col-xs-3.pull-left
      = link_to url_for([role, f.object]), method: 'delete' do
        .btn.submit
          Excluir Usuário
  .col-xs-12.col-sm-3.pull-left
    = submit_tag t('cancel'), class: 'btn'
  .col-xs-12.col-sm-3.pull-right
    = f.button :submit, t('confirm')
