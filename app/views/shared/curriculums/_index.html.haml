- role = current_role(current_user, request.url)

- content_for :stylesheet, stylesheet_link_tag('views/shared/toolbar')
- content_for :javascript, javascript_include_tag('views/shared/toolbar')

.container.content
  .row
    .col-xs-12
      .title-separator
        .media
          .media-body.media-bottom
            .media-heading.title-section
              Currículo

      .hidden-xs.hidden-sm
        = render 'shared/curriculums/toolbar'

      .row
        - curriculums.each do |curriculum|
          - object = curriculum
          - cover = image_default_path(object.current_avatar, "user")

          .col-xs-6.col-sm-3.col-md-2.no-shelf
            = link_to url_for([role, object]) do
              = image_tag(cover, class: "img-responsive", style: 'max-height: 200px; max-width: 200px; width: 100%; height: auto')
            .wine
              = object.title_str
            = object.age_str
