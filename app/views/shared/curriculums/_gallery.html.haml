#arquivos.criticas
  .title-separator
    .media
      .media-body.media-bottom
        .media-heading.title-section
          Imagens

  .row
    - curriculum.curriculum_photos.each do |photo|
      - if photo.media && photo.media.url
        = link_to photo.media.url, target: '_blank' do
          .col-xs-2{ style: "width: 240px; height: 180px;" }
            / %img.img-responsive{ src: photo.media.url, style: "width: 240px; height: 160px;" }
            = image_tag(photo.media.url, class: "img-responsive", style: 'width: 240px; height: 160px;')
