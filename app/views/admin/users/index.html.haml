%h2
  Usuários

- users.each do |user|
  .row
    .col-xs-4
      = user.name
    .col-xs-4
      = user.email
    .col-xs-2
      = link_to edit_admin_user_path(user) do
        = t('edit')
    .col-xs-2
      = link_to admin_user_path(user), method: 'delete' do
        = t('remove')
