- partial = 'admin/events/event_images/fields'

.row{'data-cocoon-container': ''}
  .col-xs-12
    %h2.title.pull-right
      = link_to_add_association '+', f, :event_images, data: { association_insertion_method: 'before' }, class: 'action-button add', partial: partial

    = f.simple_fields_for :event_images do |ep|
      = render partial, f: ep
